---
 - name: check arithmatic operators
   hosts: localhost
   gather_facts: false
   vars: 
     a: 40
     b: 60
   tasks:
     - name: "addition"
       debug:
         msg:
          - "the sum of two numbers is {{a+b}}"
